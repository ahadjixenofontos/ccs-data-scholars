library(ggplot2)
library(ggmap)
library(mapdata)
install("mapdata")
install_url("https://cran.r-project.org/bin/macosx/el-capitan/contrib/3.4/mapdata_2.2-6.tgz")
library(mapdata)
unloadNamespace('maps')
unloadNamespace('mapproj')
unloadNamespace('ggmap')
library(mapdata)
# Load the tools that understand picture files from the "exifr" library
library(exifr)
install.packages("exifr")
library(exifr)
library(exifr)
# Go to the folder where the pictures are saved
setwd("../photos/")
# Read in the picture metadata for all photos in the folder
photo_metadata <- exifr(".", recursive = TRUE)
names(photo_metadata)
write.csv("../datasets/photo-metadata.csv", quote = FALSE, row.names = FALSE)
write.csv("../datasets/photo-metadata.csv", quote = FALSE, row.names = FALSE)
write.csv("/Users/Athena/ccs-data-scholars/geospatial-project/datasets/photo-metadata.csv", quote = FALSE, row.names = FALSE)
write.csv("/Users/Athena/Desktop/photo-metadata.csv", quote = FALSE, row.names = FALSE)
write.csv(photo_metadata, "../datasets/photo-metadata.csv", quote = FALSE, row.names = FALSE)
write.csv(photo_metadata, "photo-metadata.csv", quote = FALSE, row.names = FALSE)
names(photo-metadata)
setwd("../datasets/")
# Load in the file that contains the photo medatata
photo-metadata <- read.table("photo-metadata.csv", header = TRUE, sep = ",")
setwd("../datasets/")
# Load in the file that contains the photo medatata
photo-metadata <- read.table("photo-metadata.csv", header = TRUE, sep = ",")
setwd("../datasets/")
# Load in the file that contains the photo medatata
photo_metadata <- read.table("photo-metadata.csv", header = TRUE, sep = ",")
names(photo_metadata)
# The green lines are comments. Comments help you understand what the code does.
# Comments are not code, i.e. they don't get executed by the computer.
# In other words, they have meaning for humans, not for computers.
# We use them to leave little notes to our future selves or others who will use the code.
# Go to folder with the metadata
setwd("../datasets/")
# Load in the file that contains the photo medatata
photo_metadata <- read.table("photo-metadata.csv", header = TRUE, sep = ",")
names(photo_metadata)
# Make a new table containing only the file name, latitude and longitude columns
lat_and_long <- photo_metadata[,c("FileName", "Latitude", "Longitude")]
# The green lines are comments. Comments help you understand what the code does.
# Comments are not code, i.e. they don't get executed by the computer.
# In other words, they have meaning for humans, not for computers.
# We use them to leave little notes to our future selves or others who will use the code.
# Go to folder with the metadata
setwd("../datasets/")
# Load in the file that contains the photo medatata
photo_metadata <- read.table("photo-metadata.csv", header = TRUE, sep = ",")
names(photo_metadata)
# Make a new table containing only the file name, latitude and longitude columns
lat_and_long <- photo_metadata[,c("SourceFile", "Latitude", "Longitude")]
# Update the table, keeping only the rows with complete information
lat_and_long <- lat_and_long[complete.cases(lat_and_long), ]
# Let's print it out here!
lat_and_long
# Make a new table containing only the file name, latitude and longitude columns
lat_and_long <- photo_metadata[,c("SourceFile", "Date", "Time", "Latitude", "Longitude")]
# Update the table, keeping only the rows with complete information
lat_and_long <- lat_and_long[complete.cases(lat_and_long), ]
# Let's print it out here!
lat_and_long
